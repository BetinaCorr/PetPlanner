<h1>Add a new allergy</h1>

<%= simple_form_for [@pet, @allergy] do |f| %>
  <%= f.input :name, :label => 'Nome' %>
  <%= f.input :comment, :label => 'Comentário' %>
  <%= f.button :submit, "Add this allergy" %>
<% end %>
